## 22:56 [GPT-5]
- 完成：优化套轨指标，为1小时、15分、5分、1分各周期增加最近一根已收盘K线的布林上轨、中轨、下轨价格，并在套轨面板中展示。
- 决策：沿用现有布林参数与价格格式化规则；桌面端保持单行，移动端将价格读数换行以避免溢出；同步更新数学测试与基线快照。
- 待办：无。

## 23:16 [GPT-5]
- 完成：在K线主图绘制套轨四个周期的布林上轨、中轨、下轨水平价格线，并标注周期、轨道名称和价格。
- 决策：按周期区分颜色、按轨道区分线型；将价格纳入主图比例，标签分列且限制在绘图区内；仅在已收盘轨道价格变化时整图重绘。
- 待办：基线回归受已有SMC改动影响，需在该改动稳定后重新录制或核对快照。

## 23:03 [GPT-5 Codex]
- 完成：按分钟级超短线偏差优化 SMC：POI 守住必须收盘留在区间内；结构、订单区块和 FVG 计算统一剔除未收盘 K 线；新增 `smc-bias` 回归测试并纳入全量测试。
- 决策：未擅自改变 BOS、OB、FVG 的 ATR 数值，因为现有资料只能证明需要进一步校准，不能证明某个新阈值具备行业或样本外依据。

## 23:21 [GPT-5.6]
- 完成：将指标、界面和测试变更提交为 `0818206`，并成功推送到 `origin/main`。
- 决策：项目无 `package.json`，改用仓库现有的 `node tests/run-all.mjs` 测试入口；全量测试通过。
- 待办：无。
